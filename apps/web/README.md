# Frontend web

Este directorio está reservado para la interfaz web del proyecto.

Actualmente la API Flask conserva la vista HTML dentro de `apps/api/templates`, pero este espacio está preparado para un frontend independiente en el futuro.
