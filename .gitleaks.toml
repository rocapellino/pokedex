# ==============================================================================
# Configuración de Gitleaks para Detección de Fuga de Secretos
# ==============================================================================

title = "Gitleaks Config - Pokémon DevOps Platform"

[extend]
useDefault = true

[allowlist]
description = "Permitir archivos de ejemplo, documentación y mocks de pruebas conocidos"
paths = [
  # Variables de entorno de ejemplo (nunca contienen valores reales)
  '''(?i)^\.env\.example$''',
  # Documentación técnica con snippets ilustrativos y diagramas
  '''(?i)^docs/architecture/.*\.md$''',
  '''(?i)^docs/ci-cd/.*\.md$''',
  # Tests — mocks de prueba con claves ficticias
  '''(?i)^apps/api/tests/.*''',
  '''(?i)^tests/.*''',
  # Terraform y OpenTofu — declaración de variables e infraestructura de ejemplo
  '''(?i)^infra/terraform/.*/(variables|outputs)\.tf$''',
  '''(?i)^infra/opentofu/.*/(variables|outputs|main|providers)\.tf$''',
  # K8s — SealedSecrets (cifrados con la clave pública del controlador)
  '''(?i)infra/k8s/01-sealed-secrets\.ya?ml''',
  # Ansible — solo inventario de ejemplo
  '''(?i)^infra/ansible/inventory/hosts\.ini$''',
  # Scripts de sellado documentados (contienen solo placeholders)
  '''(?i)^scripts/seal_secret\.(sh|py)$''',
  # Docker Compose — referencias a variables de entorno, no valores reales
  '''(?i)^docker-compose.*''',
]
fingerprints = [
  # Commit histórico de SealedSecret cifrado en el clúster
  "5d8c13701df243f209de41388cdc1a5d77f536d2:infra/k8s/01-sealed-secrets.yaml:generic-api-key:10",
]

