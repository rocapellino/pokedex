# ==============================================================================
# External Secrets Operator (ESO) - ClusterSecretStores Canónicos
# AWS:     ClusterSecretStore/aws-secrets-manager -> AWS Secrets Manager
# Proxmox: ClusterSecretStore/vault-backend       -> HashiCorp Vault
# ==============================================================================

apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
  labels:
    app.kubernetes.io/name: pokedex
    app.kubernetes.io/component: secrets-management
    environment: proxmox
spec:
  provider:
    vault:
      server: "https://vault.internal.lan:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "pokedex-role"
          serviceAccountRef:
            name: external-secrets-sa
            namespace: external-secrets
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
  labels:
    app.kubernetes.io/name: pokedex
    app.kubernetes.io/component: secrets-management
    environment: aws
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
            namespace: external-secrets
