# ==============================================================================
# ArgoCD Custom Health Checks - Extensión de Evaluación de CRDs (ADR-021)
# ==============================================================================
# Este ConfigMap complementa argocd-cm para proveer scripts Lua de evaluación de
# salud para recursos personalizados (CRDs) críticos de la plataforma Pokédex:
# - external-secrets.io/ExternalSecret
# - kyverno.io/ClusterPolicy
# ==============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm-healthchecks
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm-healthchecks
    app.kubernetes.io/part-of: argocd
    architecture.pokedex.io/tier: "health-monitoring"
data:
  resource.customizations.health.external-secrets.io_ExternalSecret: |
    hs = {}
    if obj.status ~= nil then
      if obj.status.conditions ~= nil then
        for i, condition in ipairs(obj.status.conditions) do
          if condition.type == "Ready" and condition.status == "True" then
            hs.status = "Healthy"
            hs.message = condition.message or "ExternalSecret sincronizado y listo"
            return hs
          end
          if condition.type == "Ready" and condition.status == "False" then
            hs.status = "Degraded"
            hs.message = condition.message or "Fallo de sincronización en ExternalSecret"
            return hs
          end
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "Esperando sincronización de ExternalSecret con el proveedor remoto"
    return hs

  resource.customizations.health.kyverno.io_ClusterPolicy: |
    hs = {}
    if obj.status ~= nil then
      if obj.status.ready == true then
        hs.status = "Healthy"
        hs.message = "ClusterPolicy activa y validando reglas de admisión"
        return hs
      end
      if obj.status.conditions ~= nil then
        for i, condition in ipairs(obj.status.conditions) do
          if condition.type == "Ready" and condition.status == "True" then
            hs.status = "Healthy"
            hs.message = condition.message or "ClusterPolicy activa"
            return hs
          end
        end
      end
    end
    hs.status = "Progressing"
    hs.message = "ClusterPolicy inicializándose en el controlador Kyverno"
    return hs
