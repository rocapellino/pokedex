# ==============================================================================
# Workflow de Seguridad SCA y Análisis de Vulnerabilidades con Trivy
# ==============================================================================
name: 🛡️ Security Vulnerability Scan (Trivy)

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'Dockerfile'
      - 'apps/**/Dockerfile'
      - 'requirements.txt'
      - '.github/workflows/security-trivy.yml'
  pull_request:
    paths:
      - 'Dockerfile'
      - 'apps/**/Dockerfile'
      - 'requirements.txt'
      - '.github/workflows/security-trivy.yml'
  schedule:
    - cron: '0 4 * * 1' # Escaneo semanal los lunes a las 04:00 UTC

jobs:
  trivy-scan:
    name: 🔍 Trivy Vulnerability & Config Scan
    runs-on: ubuntu-latest

    steps:
      - name: 1. Descargar Código Fuente
        uses: actions/checkout@v4

      - name: 2. Escaneo del Repositorio y Dependencias (SCA / FS)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0' # Permite visibilidad en el reporte sin bloquear builds en caso de falsos positivos iniciales

      - name: 3. Construir Imagen Backend para Escaneo
        run: |
          docker build -t pokedex-api:test -f apps/api/Dockerfile apps/api

      - name: 4. Escaneo de Imagen Docker Backend
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'pokedex-api:test'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
